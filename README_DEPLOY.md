# SOTE Command Portal v0.9 Approved Media Baseline - GitHub Deploy Package

This is the optimized GitHub Pages deployment package. It uses web-optimized derivatives of the recovered approved media set.

The full package preserves the original recovered approved media library.

Deploy by copying the contents of this folder into the root of the GitHub repository.
